/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Enables Next.js image optimization
    unoptimized: true, // Set to 'true' only if you want to disable image optimization for local testing
  },
};

export default nextConfig;
